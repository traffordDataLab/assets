plot.title <- list(
  family = "Roboto",
  size = 18,
  color = "#757575")

plot.subtitle <- list(
  family = "Roboto",
  size = 12,
  color = "#757575")

plot.caption <- list(
  family = "Roboto",
  size = 10,
  color = "#212121")

axis.title <- list(
  family = "Open Sans",
  size = 11,
  color = "#757575")

axis.text <- list(
  family = "Open Sans",
  size = 10,
  color = "#757575")

legend.title <- list(
  family = "Roboto",
  size = 12,
  color = "#757575")

legend.text <- list(
  family = "Roboto",
  size = 10,
  color = "#757575")
